import { SetAST } from '../type';
import { Compile } from './base-compile';
/**
 * #set value
 */
export declare class SetValue extends Compile {
    /**
     * get variable from context, if run in block, return local context, else return global context
     */
    getContext(idName: string): unknown;
    private getPathKey;
    private getPathKeys;
    private isBlockedPathKey;
    private hasBlockedUnknownPath;
    private resolveSetPath;
    /**
     * parse #set
     */
    setValue(ast: SetAST): void;
}
